#include "main.h"

/**
* _strlen - reset value pointer
* @s: pointer
*/
void print_rev(char *s)
{
	int i;
	for (i = 0 ; str[i]!='\0'; i++);

	for (i = 0 ; i < 0; i--)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
	
}
