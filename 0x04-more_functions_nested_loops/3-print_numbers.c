#include "main.h"
#include <stdio.h>

/**
 * print_numbers - check the code.
 * Return: Always 0.
 */

void print_numbers(void)
{
	int i = 0;

	while (i++ < 10)
	{
		_putchar(i + 47);
	}
		_putchar('\n');
}
